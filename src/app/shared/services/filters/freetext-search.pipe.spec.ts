import { FreetextSearchPipe } from './freetext-search.pipe';

describe('FreetextSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FreetextSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
